$(document).ready(function() {
  <% if flash[:notice] %>
    $('#flash')
      .html("<%= j(render partial: 'shared/flash') %>")
  <% else %>
    $('#purchase-form')
      .html("<%= j(render partial: 'purchase_form') %>");
  <% end %>
});
