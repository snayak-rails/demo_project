$(document).ready(function() {
  $.loadingBlockShow();
  $('#products')
    .html("<%= j(render partial: 'products', locals: { item_collection: @products }) %>")

  $('.pagination a').attr('data-remote', 'true');
  alert('loading');
  $.loadingBlockHide();
});
